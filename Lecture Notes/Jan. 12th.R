x <- c("one", "two", "three")
match.arg("o",x)
match.arg("tw",x)

strtrim("one",1)
strtrim("ThankYou",5)
as.numeric("2001")
x
strtrim(x,1)
y <- 1:3
names(y) <- x
y
# Function: tapply
tapply(data$earns74, black, mean)
tapply(data$earns74, data$edu>10)
tapply(res$Goal_Tean = team, years, sum)
# Mean Wage for Female
mean(Data$wage[Data$Female==1])

# Mean Wage FOR Male
mean(Data$wage[Data$Male==0])

plot(x,y,type = "b")
help(par)
text()
arrows
segments
points()
lines(x,y,col,type = "b")

legend(where,names, lty=1,col=1:2,) #names:("MONTREAL", "BOSTON")
# Where: "topright" or "bottomright

x <- rnorm(100)
plot(x, type = "l")
title("Random Graph")
legend("bottomleft", c("first     "), col=1, lty=2)
plot(x,type="l")
title(expression(hat(beta)==sqrt(x[2])*alpha))

sapply

beta <- sapply(1:500, function(i) lm(y~x[,i])$coef)









